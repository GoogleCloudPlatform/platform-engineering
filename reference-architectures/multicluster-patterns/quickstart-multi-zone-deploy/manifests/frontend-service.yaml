apiVersion: v1
kind: Service
metadata:
  labels:
    app: whereami-frontend
  name: whereami-frontend
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: whereami-frontend
  type: ClusterIP
---
